<p style="color: green"><%= notice %></p>

<%= render @order_line %>

<div>
  <%= link_to "Edit this order line", edit_order_line_path(@order_line) %> |
  <%= link_to "Back to order lines", order_lines_path %>

  <%= button_to "Destroy this order line", @order_line, method: :delete %>
</div>
